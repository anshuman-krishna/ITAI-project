"""classifier training over any embedding.

classifiers stay independent of the representation they consume, which keeps embedding
comparisons fair. planned baselines: logistic regression, naive bayes, svm; then
random forest, gradient boosting, and neural heads when justified.
"""

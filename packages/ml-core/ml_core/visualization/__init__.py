"""plots as research tools, not decoration.

dataset level (class distribution, length histograms), embedding level (pca, t-sne),
and evaluation level (confusion matrices, metric comparisons). every figure answers
a question and is reproducible.
"""

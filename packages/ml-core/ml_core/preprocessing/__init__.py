"""configurable text preprocessing.

small composable steps (clean, tokenize, lemmatize, normalize) combine into pipelines,
so experiments can compare preprocessing choices instead of hard-coding one path.
"""

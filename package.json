{
  "name": "itai-project",
  "version": "0.1.0",
  "private": true,
  "description": "ai-generated text detection platform",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @itai/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "workspaces": [
    "apps/web",
    "packages/shared"
  ]
}

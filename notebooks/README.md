# notebooks

Exploratory analysis and prototyping. Notebooks are for exploration, not production —
once an idea works, it moves into `packages/ml-core` where it can be tested and reused.

`.ipynb` files are gitignored to keep diffs clean; export findings to `reports/` or
promote the code into `ml-core`.
